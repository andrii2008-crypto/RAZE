services:
  - type: web
    name: raze
    env: node
    plan: free
    buildCommand: |
      npm install
      apt-get install -y python3-pip ffmpeg
      pip3 install yt-dlp
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
